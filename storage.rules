rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /uploads/{allPaths=**} {
      allow read, write: if request.auth != null
        && (request.auth.token.email == 'elliotpeep@gmail.com'
            || request.auth.token.email == 'a.fduarte1@gmail.com');
    }
  }
}
